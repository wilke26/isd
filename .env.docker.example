# Diese Werte werden von docker-compose.yml gelesen (UID/GID, DB-Zugangsdaten für den Container).
# Kopieren nach .env.docker im Projekt-Root — NICHT nach .env, das ist für
# Laravels eigene Anwendungskonfiguration reserviert (siehe .env.example).

UID=1000
GID=1000

DB_DATABASE=it_service_desk
DB_USERNAME=isd_user
DB_PASSWORD=secret
DB_ROOT_PASSWORD=rootsecret

# off | debug  – steuert Xdebug im app-Container
XDEBUG_MODE=off
